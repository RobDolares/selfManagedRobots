<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <title>Robo Directory</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>

<body>
  <header>
    <h1>User Directory</h1>
  </header>
  <main>
    <div class="currentUser">
      <h1>Welcome, {{user.name}}</h1>
    </div>
    <div class="login">
      <button><a href="/edit?id={{user._id}}">Edit Account</a></button>
      <button><a href="/logout">Log out</a></button>
    </div>
    <div id="status">
      <a href="/employed" class="employStatus">Employed robots</a>
      <a href="/unemployed" class="employStatus">Unemployed robots</a>
    </div>
    <div class="dirContainer">
      {{#users}}
      <a href="/bio/{{username}}">
        <div class="userContainer">
          <div class="headshot">
            <img src="{{avatar}}" alt="">
          </div>
          <a href="/bio/{{username}}"><h2 class="userName">{{name}}</h2></a>
          <p>{{job}}</p>
          <p>{{company}}</p>
        </div>
      {{/users}}
    </div>
  </main>
</body>

</html>
